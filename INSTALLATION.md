# Инструкция по установке проекта на новом компьютере

## Требования

- **Node.js** версии 18 или выше
- **npm** (обычно устанавливается вместе с Node.js)
- **Git** (для клонирования репозитория)

## Шаг 1: Установка Node.js (если не установлен)

### Windows:
1. Скачайте Node.js с официального сайта: https://nodejs.org/
2. Установите LTS версию (рекомендуется)
3. Проверьте установку:
   ```bash
   node --version
   npm --version
   ```

### Mac:
```bash
# Через Homebrew
brew install node

# Или скачайте с https://nodejs.org/
```

### Linux (Ubuntu/Debian):
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs
```

## Шаг 2: Клонирование проекта

```bash
# Перейдите в папку, где хотите разместить проект
cd ~/Desktop  # или любая другая папка

# Клонируйте репозиторий (замените URL на ваш)
git clone <URL_ВАШЕГО_РЕПОЗИТОРИЯ>
# ИЛИ если у вас уже есть папка проекта, просто скопируйте её

# Перейдите в папку проекта
cd modna-golovu-optom-main
```

## Шаг 3: Установка зависимостей

```bash
# Установите все зависимости проекта
npm install
```

Эта команда установит все необходимые пакеты из `package.json`:
- React и React DOM
- Vite (сборщик)
- TypeScript
- Tailwind CSS
- Supabase клиент
- Все UI компоненты (Radix UI, shadcn/ui)
- И другие зависимости

**Время установки:** 2-5 минут в зависимости от скорости интернета

## Шаг 4: Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```bash
# Windows (PowerShell)
New-Item -Path .env -ItemType File

# Mac/Linux
touch .env
```

Откройте файл `.env` и добавьте следующие переменные:

```env
VITE_SUPABASE_URL=ваш_url_из_supabase
VITE_SUPABASE_ANON_KEY=ваш_anon_key_из_supabase
```

**Где найти эти данные:**
1. Откройте ваш проект в Supabase Dashboard
2. Перейдите в Settings → API
3. Скопируйте:
   - **Project URL** → это `VITE_SUPABASE_URL`
   - **anon public key** → это `VITE_SUPABASE_ANON_KEY`

**Пример:**
```env
VITE_SUPABASE_URL=https://abcdefghijklmnop.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImFiY2RlZmdoaWprbG1ub3AiLCJyb2xlIjoiYW5vbiIsImlhdCI6MTYxNjIzOTAyMiwiZXhwIjoxOTMxODE1MDIyfQ.abcdefghijklmnopqrstuvwxyz1234567890
```

## Шаг 5: Запуск проекта

```bash
# Запустите development сервер
npm run dev
```

После запуска вы увидите что-то вроде:
```
  VITE v5.4.19  ready in 500 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

Откройте браузер и перейдите по адресу: **http://localhost:5173**

## Дополнительные команды

```bash
# Сборка для production
npm run build

# Просмотр production сборки локально
npm run preview

# Проверка кода на ошибки
npm run lint
```

## Возможные проблемы и решения

### Проблема: "npm: command not found"
**Решение:** Установите Node.js (см. Шаг 1)

### Проблема: "EACCES: permission denied"
**Решение (Mac/Linux):**
```bash
sudo chown -R $(whoami) ~/.npm
```

### Проблема: Ошибки при установке зависимостей
**Решение:**
```bash
# Очистите кэш npm
npm cache clean --force

# Удалите папку node_modules и package-lock.json
rm -rf node_modules package-lock.json

# Установите заново
npm install
```

### Проблема: Порт 5173 уже занят
**Решение:**
```bash
# Используйте другой порт
npm run dev -- --port 3000
```

### Проблема: Белый экран в браузере
**Решение:**
1. Проверьте, что файл `.env` создан и содержит правильные данные
2. Перезапустите dev сервер после создания `.env`
3. Проверьте консоль браузера (F12) на наличие ошибок

## Быстрая установка (для опытных пользователей)

```bash
# 1. Клонируйте проект
git clone <URL_РЕПОЗИТОРИЯ>
cd modna-golovu-optom-main

# 2. Установите зависимости
npm install

# 3. Создайте .env файл с переменными Supabase
echo "VITE_SUPABASE_URL=ваш_url" > .env
echo "VITE_SUPABASE_ANON_KEY=ваш_key" >> .env

# 4. Запустите проект
npm run dev
```

## Структура проекта

```
modna-golovu-optom-main/
├── src/                    # Исходный код
│   ├── components/        # React компоненты
│   ├── pages/            # Страницы приложения
│   ├── services/         # Сервисы для работы с API
│   ├── types/            # TypeScript типы
│   └── lib/              # Утилиты и конфигурация
├── public/               # Статические файлы
├── node_modules/         # Установленные зависимости (не коммитится)
├── .env                  # Переменные окружения (не коммитится)
├── package.json          # Зависимости проекта
└── vite.config.ts        # Конфигурация Vite
```

## Полезные ссылки

- [Документация Vite](https://vitejs.dev/)
- [Документация React](https://react.dev/)
- [Документация Supabase](https://supabase.com/docs)
- [Документация Tailwind CSS](https://tailwindcss.com/docs)

## Контакты для поддержки

Если возникли проблемы при установке, проверьте:
1. Версию Node.js (должна быть 18+)
2. Наличие файла `.env` с правильными данными
3. Консоль браузера на наличие ошибок (F12 → Console)

---

**Удачной работы! 🚀**








