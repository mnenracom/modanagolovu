# Инструкция по деплою на Vercel

## Подготовка проекта

Перед деплоем убедитесь, что:

1. ✅ Все изменения закоммичены в Git
2. ✅ Проект успешно собирается локально (`npm run build`)
3. ✅ Файл `.env.example` существует (для примера переменных окружения)

## Шаг 1: Создание репозитория на GitHub

1. Перейдите на [GitHub](https://github.com) и войдите в свой аккаунт
2. Нажмите на "+" в правом верхнем углу → "New repository"
3. Заполните:
   - **Repository name**: `modna-golovu-optom` (или любое другое имя)
   - **Description**: "Интернет-магазин головных уборов оптом"
   - **Visibility**: Public или Private (на ваше усмотрение)
   - **НЕ** добавляйте README, .gitignore или license (они уже есть в проекте)
4. Нажмите "Create repository"

## Шаг 2: Подключение локального репозитория к GitHub

Откройте терминал в корне проекта и выполните:

```bash
# Проверяем, что Git инициализирован (если нет - будет ошибка)
git status

# Если Git еще не инициализирован:
git init

# Добавляем все файлы
git add .

# Делаем первый коммит
git commit -m "Initial commit: интернет-магазин с админ-панелью"

# Добавляем удаленный репозиторий (замените YOUR_USERNAME и REPO_NAME)
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# Отправляем код на GitHub
git branch -M main
git push -u origin main
```

**Примечание**: Если GitHub попросит авторизацию, используйте Personal Access Token вместо пароля.

## Шаг 3: Деплой на Vercel

### Вариант 1: Через веб-интерфейс Vercel

1. Перейдите на [Vercel](https://vercel.com) и войдите (можно через GitHub)
2. Нажмите "Add New..." → "Project"
3. Импортируйте ваш репозиторий с GitHub
4. Настройте проект:
   - **Framework Preset**: Vite (должно определиться автоматически)
   - **Root Directory**: `./` (по умолчанию)
   - **Build Command**: `npm run build` (по умолчанию)
   - **Output Directory**: `dist` (по умолчанию)
   - **Install Command**: `npm install` (по умолчанию)

5. **Важно! Добавьте переменные окружения:**
   - Перейдите в "Environment Variables"
   - Добавьте:
     - `VITE_SUPABASE_URL` = ваш Supabase URL
     - `VITE_SUPABASE_ANON_KEY` = ваш Supabase Anon Key
   - Выберите все окружения (Production, Preview, Development)

6. Нажмите "Deploy"

### Вариант 2: Через Vercel CLI

```bash
# Установите Vercel CLI глобально
npm i -g vercel

# Войдите в Vercel
vercel login

# Деплой
vercel

# Для production деплоя
vercel --prod
```

При первом деплое Vercel спросит:
- Set up and deploy? **Yes**
- Which scope? Выберите ваш аккаунт
- Link to existing project? **No**
- Project name? Оставьте по умолчанию или укажите свое
- Directory? `./`
- Override settings? **No**

## Шаг 4: Настройка переменных окружения на Vercel

После деплоя:

1. Перейдите в Dashboard Vercel → ваш проект → Settings → Environment Variables
2. Добавьте:
   - `VITE_SUPABASE_URL` = `https://your-project.supabase.co`
   - `VITE_SUPABASE_ANON_KEY` = ваш ключ
3. Выберите все окружения
4. Нажмите "Save"
5. Передеплойте проект (Deployments → последний деплой → "Redeploy")

## Шаг 5: Настройка домена (опционально)

1. В Vercel Dashboard → Settings → Domains
2. Добавьте ваш домен
3. Следуйте инструкциям по настройке DNS

## Проверка после деплоя

После успешного деплоя:

1. ✅ Откройте сайт по ссылке от Vercel
2. ✅ Проверьте, что главная страница загружается
3. ✅ Проверьте работу авторизации
4. ✅ Проверьте админ-панель (если есть доступ)

## Обновление проекта

Для обновления сайта:

```bash
# Внесите изменения в код
# ...

# Закоммитьте и отправьте на GitHub
git add .
git commit -m "Описание изменений"
git push

# Vercel автоматически задеплоит новую версию!
```

## Полезные команды

```bash
# Посмотреть логи деплоя
vercel logs

# Посмотреть информацию о проекте
vercel inspect

# Открыть проект в браузере
vercel open
```

## Возможные проблемы

### Ошибка: "Failed to fetch" или Supabase не подключается

**Решение**: Убедитесь, что переменные окружения добавлены в Vercel и проект передеплоен.

### Ошибка при сборке

**Решение**: 
1. Проверьте, что проект собирается локально: `npm run build`
2. Проверьте логи деплоя в Vercel Dashboard
3. Убедитесь, что все зависимости указаны в `package.json`

### Страница показывает 404 для всех роутов

**Решение**: Убедитесь, что в `vercel.json` есть rewrites для SPA роутинга.

## Дополнительные настройки

### Автоматический деплой

Vercel автоматически деплоит при каждом push в:
- `main`/`master` → Production
- Другие ветки → Preview

### Environment Variables для разных окружений

В Vercel можно настроить разные переменные для:
- Production
- Preview
- Development

---

**Готово! Ваш сайт теперь доступен по адресу от Vercel! 🚀**




