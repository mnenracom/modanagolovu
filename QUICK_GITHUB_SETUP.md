# 🚀 Быстрая инструкция: Загрузка на GitHub и деплой на Vercel

## ⚡ Быстрый старт (5 минут)

### 1. Подготовка (2 минуты)

```bash
# Проверьте, что .env НЕ будет загружен
git status | grep .env

# Если .env есть в списке - проверьте .gitignore
```

### 2. Инициализация Git (30 секунд)

```bash
git init
git add .
git commit -m "Initial commit: интернет-магазин с админ-панелью"
```

### 3. Создание репозитория на GitHub (1 минута)

1. Перейдите на https://github.com/new
2. Имя: `modna-golovu-optom` (или любое)
3. **НЕ** добавляйте README, .gitignore, license
4. Нажмите "Create repository"

### 4. Загрузка кода (1 минута)

```bash
# Замените YOUR_USERNAME и REPO_NAME на ваши
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git
git branch -M main
git push -u origin main
```

### 5. Деплой на Vercel (1 минута)

1. Перейдите на https://vercel.com
2. "Add New..." → "Project"
3. Импортируйте репозиторий
4. Добавьте переменные окружения:
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`
5. Нажмите "Deploy"

## 📝 Подробные инструкции

- **Для GitHub**: см. `GITHUB_SETUP.md`
- **Для Vercel**: см. `DEPLOY_VERCEL.md`

## ✅ Чек-лист перед загрузкой

- [ ] Файл `.env` НЕ в списке файлов для коммита
- [ ] `node_modules/` исключен через .gitignore
- [ ] `dist/` исключен через .gitignore
- [ ] Создан файл `.env.example` (опционально)
- [ ] README.md обновлен

## ⚠️ Важно!

**НЕ загружайте файл `.env` с реальными ключами в репозиторий!**

Если это произошло:
1. Немедленно смените ключи в Supabase
2. Удалите файл из Git: `git rm --cached .env`
3. Добавьте в `.gitignore` (уже добавлено)

---

**Готово! Проект на GitHub и Vercel! 🎉**




