# Инструкция по подготовке проекта для GitHub

## ✅ Чек-лист перед загрузкой на GitHub

### 1. Проверка файлов

Убедитесь, что все необходимые файлы на месте:

- ✅ `package.json` - зависимости проекта
- ✅ `vite.config.ts` - конфигурация Vite
- ✅ `tailwind.config.ts` - конфигурация Tailwind
- ✅ `.gitignore` - игнорируемые файлы
- ✅ `README.md` - описание проекта
- ✅ `vercel.json` - конфигурация для Vercel
- ✅ `src/` - исходный код
- ✅ `public/` - статические файлы

### 2. Проверка безопасности

⚠️ **ВАЖНО! Проверьте, что в репозиторий НЕ попадут:**

- ❌ Файл `.env` с реальными ключами (должен быть в `.gitignore`)
- ❌ API ключи, секреты, пароли в коде
- ❌ Личные данные
- ❌ Файлы базы данных

✅ **Проверьте `.gitignore`:**
```bash
# Убедитесь, что в .gitignore есть:
.env
.env.local
.env.production
node_modules/
dist/
```

### 3. Создание файла .env.example

Создайте файл `.env.example` вручную:

**Windows (PowerShell):**
```powershell
@"
# Supabase Configuration
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
"@ | Out-File -FilePath .env.example -Encoding utf8
```

**Mac/Linux:**
```bash
cat > .env.example << EOF
# Supabase Configuration
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
EOF
```

Или создайте файл вручную через редактор.

## 📤 Загрузка на GitHub

### Шаг 1: Инициализация Git (если еще не сделано)

```bash
# Проверяем статус Git
git status

# Если Git не инициализирован:
git init
```

### Шаг 2: Проверка файлов перед коммитом

```bash
# Посмотреть, какие файлы будут добавлены
git status

# Проверьте, что .env НЕ в списке!
```

### Шаг 3: Добавление файлов

```bash
# Добавляем все файлы (кроме тех, что в .gitignore)
git add .
```

### Шаг 4: Первый коммит

```bash
git commit -m "Initial commit: интернет-магазин с админ-панелью и интеграцией Supabase"
```

### Шаг 5: Создание репозитория на GitHub

1. Откройте [GitHub](https://github.com)
2. Нажмите "+" → "New repository"
3. Заполните:
   - **Repository name**: `modna-golovu-optom` (или другое имя)
   - **Description**: "Интернет-магазин головных уборов оптом"
   - **Visibility**: Public или Private
   - ❌ **НЕ** добавляйте README, .gitignore, license (они уже есть)

### Шаг 6: Подключение к удаленному репозиторию

```bash
# Добавляем удаленный репозиторий (замените YOUR_USERNAME и REPO_NAME)
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# Проверяем подключение
git remote -v
```

### Шаг 7: Отправка кода на GitHub

```bash
# Переименовываем ветку в main (если нужно)
git branch -M main

# Отправляем код
git push -u origin main
```

**Если GitHub запросит авторизацию:**

1. Создайте Personal Access Token:
   - GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
   - Generate new token (classic)
   - Выберите scopes: `repo`
   - Скопируйте токен

2. Используйте токен как пароль при `git push`

## 🔍 Проверка после загрузки

После успешной загрузки:

1. ✅ Откройте репозиторий на GitHub
2. ✅ Убедитесь, что файлы на месте
3. ✅ Проверьте, что `.env` НЕ в репозитории
4. ✅ Проверьте, что `node_modules/` НЕ в репозитории
5. ✅ Проверьте, что `dist/` НЕ в репозитории

## 🚀 Следующий шаг: Деплой на Vercel

После загрузки на GitHub следуйте инструкции в файле `DEPLOY_VERCEL.md`.

## ❓ Возможные проблемы

### Ошибка: "remote origin already exists"

**Решение:**
```bash
git remote remove origin
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git
```

### Ошибка: "Authentication failed"

**Решение:**
- Используйте Personal Access Token вместо пароля
- Или настройте SSH ключи

### Ошибка: "branch main does not exist"

**Решение:**
```bash
git branch -M main
git push -u origin main
```

### Файл .env попал в репозиторий

**Решение (если это произошло):**
```bash
# Удалите файл из Git (но оставьте локально)
git rm --cached .env

# Закоммитьте изменение
git commit -m "Remove .env from repository"

# Отправьте на GitHub
git push

# Важно: В Supabase Dashboard измените все ключи!
```

---

**Готово! Проект загружен на GitHub! 🎉**




