# Управление темами оформления сайта

## Описание

Система управления темами позволяет включать и выключать праздничные оформления сайта через админ-панель. Темы добавляют декоративные элементы (снежинки, цветы, бабочки и т.д.) на все страницы сайта.

## Доступные темы

### 1. Обычная тема (none)
- Стандартное оформление без праздничных эффектов
- Используется по умолчанию

### 2. Новогодняя тема (newyear)
- Падающие снежинки
- Блестящие частицы
- Новогодняя гирлянда
- Праздничные градиенты и рамки

### 3. Весенняя тема (spring)
- Падающие лепестки цветов
- Летающие бабочки
- Блестящие частицы
- Весенняя гирлянда
- Нежные пастельные цвета

## Как использовать

### Включение/выключение темы

1. Войдите в админ-панель
2. Перейдите в **Настройки** → **Темы оформления**
3. Выберите нужную тему, кликнув на карточку
4. Тема применится сразу на всех страницах сайта

### Добавление новой темы

1. Создайте новый компонент декораций в `src/components/` (например, `SummerDecorations.tsx`)
2. Добавьте тему в `src/types/themes.ts`:
   ```typescript
   {
     id: 'summer',
     name: 'Летняя',
     description: 'Солнце, море и летнее настроение',
     icon: '☀️',
     enabled: true,
   }
   ```
3. Добавьте стили в `src/index.css` для новой темы
4. Импортируйте и добавьте компонент в `src/App.tsx`
5. Обновите `Header.tsx` и `ProductCard.tsx` для поддержки новой темы

## Технические детали

### Хранение настроек

Активная тема хранится в таблице `settings` в Supabase с ключом `active_theme`.

### Инициализация

При первом запуске, если настройка отсутствует, используется тема `none` по умолчанию.

### SQL скрипт

Для добавления настройки в базу данных выполните:
```sql
-- См. файл add_theme_setting.sql
```

## Особенности

- Темы не отображаются в админ-панели (только на публичных страницах)
- Изменения применяются мгновенно без перезагрузки страницы
- Все эффекты оптимизированы для производительности
- Темы адаптивны и работают на всех устройствах

## Примеры использования

### Новогодний период
- Включите новогоднюю тему с 1 декабря по 15 января

### Весенний период
- Включите весеннюю тему с 1 марта по 31 мая

### Обычное время
- Используйте обычную тему в остальное время

---

**Приятной работы с темами! 🎨**

