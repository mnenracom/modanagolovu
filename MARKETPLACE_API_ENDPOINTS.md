# API Endpoints для синхронизации товаров с маркетплейсов

## Обзор

Для синхронизации товаров с маркетплейсов используются следующие API endpoints. Все данные о товарах (название, цена, артикул, категория и т.д.) получаются автоматически через эти API.

---

## WildBerries API

### Базовый URL
- **Statistics API:** `https://statistics-api.wildberries.ru` (используется для остатков)
- **Marketplace API:** `https://suppliers-api.wildberries.ru` (для полной информации о товарах)

### Endpoints для синхронизации товаров

#### 1. `/api/v1/supplier/stocks` (Statistics API)
**Метод:** GET  
**Используется для:** Получения остатков товаров на складах  
**Текущее использование:** Основной endpoint для синхронизации товаров WB

**Что возвращает:**
- `nmId` - ID товара (номенклатура)
- `name` / `subject` - Название товара
- `brand` - Бренд
- `price` - Цена без скидки
- `priceWithDisc` / `price_with_disc` - Цена со скидкой
- `quantity` / `qty` - Количество на складе
- `category` / `subject` - Категория товара
- `discount` - Размер скидки

**Ограничения:**
- Не содержит полную информацию о товаре (штрихкод, фото, рейтинг и т.д.)
- Не содержит `supplierArticle` (артикул поставщика)

**Рекомендация:** Для получения полной информации лучше использовать Marketplace API.

---

#### 2. `/api/v1/supplier/cards/list` (Marketplace API) ⚠️ НЕ ИСПОЛЬЗУЕТСЯ СЕЙЧАС
**Метод:** POST  
**Используется для:** Получения полного списка карточек товаров  
**Статус:** Упомянут в коде, но не реализован

**Что должен возвращать:**
- Полная информация о товарах
- `supplierArticle` - Артикул поставщика (важно для идентификации)
- `barcode` - Штрихкод
- `photos` - Фотографии товара
- `rating` - Рейтинг
- `feedbacks` - Количество отзывов
- И другие детали товара

**Почему не используется:**
- Требует отдельный токен для Marketplace API (не Statistics API)
- Текущая реализация использует Statistics API через прокси

**Для улучшения:** Можно добавить поддержку Marketplace API для получения полной информации.

---

## OZON API

### Базовый URL
`https://api-seller.ozon.ru`

### Endpoints для синхронизации товаров

#### 1. `/v3/product/list` (Seller API)
**Метод:** POST  
**Используется для:** Получения списка всех товаров продавца  
**Текущее использование:** Основной endpoint для синхронизации товаров OZON

**Параметры запроса:**
```json
{
  "filter": {
    "visibility": "ALL"  // Все товары (включая скрытые)
  },
  "limit": 100,         // Количество товаров за запрос
  "offset": 0,          // Смещение для пагинации
  "last_id": "",        // ID последнего товара (для пагинации)
  "sort_dir": "ASC"     // Направление сортировки
}
```

**Что возвращает:**
- `product_id` - ID товара на OZON
- `offer_id` - Артикул товара (SKU)
- `name` - Название товара
- `barcode` / `barcodes[]` - Штрихкод(ы)
- `price` - Текущая цена
- `old_price` - Старая цена (если есть скидка)
- `category_id` - ID категории
- `category_name` - Название категории
- `stocks` - Остатки на складе:
  - `present` - В наличии
  - `coming` - В пути
  - `reserved` - Зарезервировано
- `status` - Статус товара
- `visible` - Видимость товара
- `images[]` - Массив URL изображений
- `currency_code` - Валюта
- И много другой информации

**Особенности:**
- Поддерживает пагинацию (нужно делать несколько запросов для получения всех товаров)
- Возвращает до 100 товаров за запрос
- Требует авторизацию через `Client-Id` и `Api-Key`

---

## Сравнительная таблица

| Маркетплейс | Endpoint | Метод | Что получаем | Ограничения |
|------------|----------|-------|--------------|-------------|
| **WildBerries** | `/api/v1/supplier/stocks` | GET | Остатки, цены, названия, категории | Нет штрихкода, фото, рейтинга |
| **WildBerries** | `/api/v1/supplier/cards/list` | POST | Полная информация о товарах | Не реализован, требует другой токен |
| **OZON** | `/v3/product/list` | POST | Полная информация о товарах | Требует пагинацию для больших каталогов |

---

## Какие данные извлекаются из каждого API

### WildBerries (из `/api/v1/supplier/stocks`)

Извлекаемые данные:
- ✅ **ID товара** (`nmId`) → `marketplaceProductId`
- ✅ **Название** (`name` / `subject`) → `name`
- ✅ **Цена** (`price`, `priceWithDisc`) → `price`, `oldPrice`
- ✅ **Остатки** (`quantity`) → `stockQuantity`
- ✅ **Категория** (`category` / `subject`) → `category`
- ✅ **Бренд** (`brand`) → `metadata.brand`
- ❌ **Штрихкод** - не доступен в этом endpoint
- ❌ **Фото** - не доступны в этом endpoint
- ❌ **Рейтинг** - не доступен в этом endpoint
- ❌ **Артикул поставщика** (`supplierArticle`) - не доступен

**Используется как SKU:** `nmId` (ID товара)

---

### OZON (из `/v3/product/list`)

Извлекаемые данные:
- ✅ **ID товара** (`product_id`) → `marketplaceProductId`
- ✅ **Артикул** (`offer_id`) → `sku`
- ✅ **Название** (`name`) → `name`
- ✅ **Штрихкод** (`barcode` / `barcodes[0]`) → `barcode`
- ✅ **Цена** (`price`) → `price`
- ✅ **Старая цена** (`old_price`) → `oldPrice`
- ✅ **Остатки** (`stocks.present`) → `stockQuantity`
- ✅ **Категория** (`category_name`) → `category`
- ✅ **Фото** (`images[]`) → `images`
- ✅ **Статус** (`status`) → `status`
- ✅ **Видимость** (`visible`) → `isVisible`
- ✅ **ID категории** (`category_id`) → `metadata.categoryId`

**Используется как SKU:** `offer_id` (артикул товара)

---

## Рекомендации по улучшению

### Для WildBerries:

1. **Добавить поддержку Marketplace API** (`/api/v1/supplier/cards/list`)
   - Получит полную информацию о товарах
   - Включая `supplierArticle` (артикул поставщика)
   - Включая штрихкоды, фото, рейтинги

2. **Альтернатива:** Использовать комбинацию endpoints:
   - `/api/v1/supplier/stocks` - для остатков и цен
   - `/api/v1/supplier/cards/list` - для полной информации о товарах

### Для OZON:

1. **Уже хорошо работает** - endpoint `/v2/product/list` возвращает всю необходимую информацию
2. Можно добавить кэширование для оптимизации частых запросов

---

## Требования к API ключам

### WildBerries:
- **Тип токена:** Statistics API Token
- **Права:** Доступ к остаткам (`/api/v1/supplier/stocks`)
- **Для полной информации:** Нужен отдельный токен для Marketplace API

### OZON:
- **Тип ключа:** API Key + Client ID
- **Права:** Доступ к списку товаров (`/v2/product/list`)
- **Обычно:** Стандартные ключи продавца имеют эти права

---

## Итоговая информация

**Текущая реализация использует:**
1. **WildBerries:** `/api/v1/supplier/stocks` (Statistics API)
2. **OZON:** `/v3/product/list` (Seller API)

**Что получаем автоматически:**
- ✅ Название товара
- ✅ Текущая цена
- ✅ Артикул (SKU)
- ✅ Категория
- ✅ Остатки на складе
- ✅ ID товара на маркетплейсе
- ✅ (OZON) Штрихкод, фото, статус

**Что нужно заполнить вручную (через Excel):**
- 💰 Себестоимость
- 📊 Минимальная цена
- 📊 Максимальная цена
- 📊 Целевая маржа (опционально)

