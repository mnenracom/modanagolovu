# 📋 Инструкция по настройке новых функций

## ✅ Что реализовано

### 1. 🔔 Уведомления о поступлении товара (Back in Stock)
- Подписка на уведомления о появлении товара в наличии
- Уведомления через Email и Telegram
- Автоматическая отправка при изменении статуса товара

### 2. 📦 Отслеживание заказа (Order Tracking)
- Страница отслеживания по номеру заказа (`/tracking`)
- Визуализация статусов доставки
- Интеграция с настраиваемыми службами доставки

### 3. 🚚 Настройка служб доставки (в админке)
- Подключение/отключение служб доставки
- Настройка API ключей
- Быстрое добавление популярных служб (СДЭК, Почта России, Яндекс Доставка, Boxberry)

### 4. 💳 Настройка платежных систем (в админке)
- Подключение/отключение платежных систем
- Настройка API ключей
- Быстрое добавление популярных систем (ЮKassa, Сбербанк, Тинькофф, Stripe)

---

## 🚀 Шаги по настройке

### Шаг 1: Создание таблиц в базе данных

Выполните следующие SQL скрипты в Supabase Dashboard → SQL Editor:

1. **Уведомления о поступлении товара:**
   ```sql
   -- Выполните содержимое файла create_product_notifications_table.sql
   ```

2. **Службы доставки:**
   ```sql
   -- Выполните содержимое файла create_delivery_services_table.sql
   ```

3. **Платежные системы:**
   ```sql
   -- Выполните содержимое файла create_payment_gateways_table.sql
   ```

4. **Обновление таблицы orders:**
   ```sql
   -- Выполните содержимое файла update_orders_table_for_tracking.sql
   ```

### Шаг 2: Настройка служб доставки

1. Перейдите в админ-панель → **Службы доставки** (`/admin/delivery`)
2. Нажмите на кнопку с нужной службой доставки (например, "СДЭК")
3. Нажмите кнопку **Редактировать** (иконка карандаша)
4. Заполните:
   - **API ключ** - ваш API ключ от службы доставки
   - **Секретный ключ** - секретный ключ (если нужен)
   - **ID аккаунта** - ID вашего аккаунта
   - **ID города отправителя** - ID города, откуда отправляете
   - **Адрес отправителя** - полный адрес склада
   - **URL API** - URL API службы (обычно уже заполнен)
   - **Endpoint для отслеживания** - endpoint для получения статуса (например, `/v2/orders/{tracking_number}`)
5. Включите переключатель **Включена** для активации службы
6. Нажмите **Сохранить**

### Шаг 3: Настройка платежных систем

1. Перейдите в админ-панель → **Платежные системы** (`/admin/payments`)
2. Нажмите на кнопку с нужной платежной системой (например, "ЮKassa")
3. Нажмите кнопку **Редактировать** (иконка карандаша)
4. Заполните:
   - **Публичный ключ (API Key)** - ваш публичный ключ
   - **Секретный ключ** - секретный ключ
   - **ID магазина (Shop ID)** - ID вашего магазина
   - **URL API** - URL API платежной системы (обычно уже заполнен)
   - **URL возврата** - URL, куда вернется пользователь после оплаты (например, `https://ваш-сайт.ru/checkout/success`)
   - **URL при ошибке** - URL при ошибке оплаты (например, `https://ваш-сайт.ru/checkout/error`)
5. Если нужно тестирование, включите **Тестовый режим** и заполните тестовые ключи
6. Включите переключатель **Включена** для активации системы
7. Нажмите **Сохранить**

### Шаг 4: Проверка работы

#### Уведомления о поступлении:
1. Откройте страницу товара, который не в наличии
2. Нажмите кнопку **"Уведомить о поступлении"**
3. Введите email
4. В админ-панели измените статус товара на "в наличии"
5. Проверьте, что уведомление отправлено (через Telegram, если настроен)

#### Отслеживание заказа:
1. Создайте тестовый заказ
2. В админ-панели → Заказы откройте заказ
3. Укажите номер отслеживания и выберите службу доставки
4. Перейдите на страницу `/tracking?order=НОМЕР_ЗАКАЗА`
5. Проверьте отображение статуса доставки

---

## 📝 Примечания

### Службы доставки

- **СДЭК**: Требуется регистрация на https://cdek.ru и получение API ключей
- **Почта России**: Требуется регистрация на https://otpravka.pochta.ru
- **Яндекс Доставка**: Требуется регистрация в Яндекс.Доставке
- **Boxberry**: Требуется регистрация на https://boxberry.ru

### Платежные системы

- **ЮKassa**: Требуется регистрация на https://yookassa.ru и создание магазина
- **Сбербанк**: Требуется подключение к Сбербанк Онлайн
- **Тинькофф**: Требуется регистрация в Тинькофф Банке
- **Stripe**: Требуется регистрация на https://stripe.com

### Важно

- Все API ключи хранятся в базе данных в открытом виде. Для продакшена рекомендуется использовать переменные окружения или шифрование.
- Тестовый режим для платежных систем позволяет тестировать без реальных платежей.
- Службы доставки можно включать/отключать без удаления настроек.

---

## 🔧 Дополнительная настройка

### Интеграция с API служб доставки

Для полной интеграции с API служб доставки нужно реализовать методы в `deliveryServicesService.getTrackingStatus()`. Сейчас там заглушка.

Пример для СДЭК:
```typescript
// В src/services/deliveryServicesService.ts
async getTrackingStatus(serviceCode: string, trackingNumber: string) {
  if (serviceCode === 'cdek') {
    // Интеграция с API СДЭК
    const response = await fetch(`${service.apiUrl}/v2/orders/${trackingNumber}`, {
      headers: {
        'Authorization': `Bearer ${service.apiKey}`,
      },
    });
    const data = await response.json();
    // Преобразование ответа API в наш формат
    return {
      status: data.status,
      history: data.history,
      location: data.location,
    };
  }
  // ... другие службы
}
```

### Интеграция с платежными системами

Для полной интеграции с платежными системами нужно реализовать методы создания платежей и обработки webhook'ов.

---

## ❓ Вопросы и поддержка

Если возникли проблемы:
1. Проверьте, что все SQL скрипты выполнены
2. Проверьте, что API ключи введены правильно
3. Проверьте логи в консоли браузера
4. Проверьте настройки Telegram бота (если используете уведомления)




