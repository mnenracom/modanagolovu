# Деплой Supabase Edge Function для проксирования запросов к WildBerries API

## ⚠️ Важно: Это не помешает при переносе на сервер!

**Edge Functions — это правильный подход для продакшена:**
- ✅ Работают на серверах Supabase (не в браузере)
- ✅ Обходят CORS ограничения
- ✅ Безопасно хранят API ключи (не видны в браузере)
- ✅ Масштабируются автоматически
- ✅ Работают на Free плане (до 500,000 вызовов/месяц)
- ✅ Можно использовать и на продакшене

**При переносе на сервер ничего менять не нужно** — Edge Function будет работать так же, как и сейчас.

---

## 📋 Пошаговая инструкция по деплою

### Шаг 1: Установка Supabase CLI

**⚠️ Важно:** Установка через `npm install -g` не поддерживается! Используйте один из способов ниже.

#### Способ 1: Через Scoop (рекомендуется для Windows)

1. Если у вас установлен Scoop, выполните:
   ```powershell
   scoop bucket add supabase https://github.com/supabase/scoop-bucket.git
   scoop install supabase
   ```

2. Если Scoop не установлен, сначала установите его:
   ```powershell
   Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
   irm get.scoop.sh | iex
   ```
   Затем установите Supabase CLI (команды выше).

#### Способ 2: Через npx (без установки, временное использование)

Можно использовать Supabase CLI через `npx` без установки:

```bash
npx supabase@latest login
npx supabase@latest link --project-ref YOUR_PROJECT_REF
npx supabase@latest functions deploy wildberries-proxy
```

**Минус:** Нужно добавлять `npx` перед каждой командой.

#### Способ 3: Прямая загрузка (Windows)

1. Скачайте последнюю версию для Windows:
   - Перейдите на https://github.com/supabase/cli/releases
   - Скачайте файл `supabase_windows_amd64.zip` (или `supabase_windows_arm64.zip` для ARM)

2. Распакуйте архив

3. Переместите `supabase.exe` в папку, которая есть в PATH (например, `C:\Windows\System32`)

4. Или добавьте папку с `supabase.exe` в переменную PATH:
   - Откройте "Переменные среды" (Environment Variables)
   - Добавьте путь к папке с `supabase.exe` в PATH

#### Проверка установки

После установки любым способом проверьте:
```bash
supabase --version
```
Должна отобразиться версия (например, `1.200.0`)

---

### Шаг 2: Вход в Supabase

1. Войдите в Supabase через браузер:
   ```bash
   npx supabase@latest login
   ```
   (Если установили через Scoop или прямую загрузку, используйте просто `supabase login`)
   
2. Команда откроет браузер — войдите в свой аккаунт Supabase
3. После входа вернитесь в терминал — должно появиться сообщение об успешном входе

---

### Шаг 3: Получение Project Reference

1. Откройте [Supabase Dashboard](https://app.supabase.com/)
2. Выберите ваш проект
3. Перейдите в **Settings** → **General**
4. Найдите **Reference ID** (например, `abcdefghijklmnop`)
5. Скопируйте этот ID — он понадобится в следующем шаге

---

### Шаг 4: Связывание проекта

1. В терминале перейдите в папку проекта:
   ```bash
   cd S:\modna-golovu-optom-main
   ```

2. Свяжите локальный проект с Supabase проектом:
   ```bash
   npx supabase@latest link --project-ref YOUR_PROJECT_REF
   ```
   (Если установили через Scoop или прямую загрузку, используйте просто `supabase link`)
   
   Замените `YOUR_PROJECT_REF` на Reference ID из шага 3

3. При запросе выберите организацию (organization) и подтвердите

---

### Шаг 5: Деплой Edge Function

1. Убедитесь, что файл функции существует:
   ```
   supabase/functions/wildberries-proxy/index.ts
   ```

2. Задеплойте функцию:
   ```bash
   npx supabase@latest functions deploy wildberries-proxy
   ```
   (Если установили через Scoop или прямую загрузку, используйте просто `supabase functions deploy`)

3. Дождитесь завершения деплоя — должно появиться сообщение:
   ```
   Deployed Function wildberries-proxy
   ```

---

### Шаг 6: Проверка деплоя

1. Проверьте список задеплоенных функций:
   ```bash
   npx supabase@latest functions list
   ```
   (Если установили через Scoop или прямую загрузку, используйте просто `supabase functions list`)

2. Должна отобразиться функция `wildberries-proxy` со статусом `ACTIVE`

3. Также можно проверить в Supabase Dashboard:
   - Перейдите в **Edge Functions** в боковом меню
   - Должна быть видна функция `wildberries-proxy`

---

### Шаг 7: Тестирование

1. Откройте ваше приложение в браузере
2. Перейдите в админ-панель → **Маркетплейсы**
3. Добавьте или выберите аккаунт WildBerries
4. Нажмите **"Синхронизировать"**
5. Проверьте консоль браузера (F12) — не должно быть CORS ошибок
6. Если всё работает, данные должны синхронизироваться успешно

---

## 🔧 Локальная разработка (опционально)

Если хотите тестировать функцию локально перед деплоем:

1. Запустите локальный Supabase:
   ```bash
   supabase start
   ```

2. Запустите функцию локально:
   ```bash
   supabase functions serve wildberries-proxy
   ```

3. Функция будет доступна по адресу:
   ```
   http://localhost:54321/functions/v1/wildberries-proxy
   ```

4. Для тестирования с локальной функцией временно измените в коде:
   ```typescript
   // В src/services/wildberriesApiService.ts
   const USE_PROXY = false; // Временно отключить для локального теста
   ```

---

## 📊 Лимиты Free плана

- **500,000 вызовов функций в месяц** — достаточно для синхронизации несколько раз в день
- **2 GB исходящего трафика в месяц** — более чем достаточно
- **20 секунд максимальное время выполнения** — достаточно для API запросов

**Если превысите лимиты**, можно перейти на Pro план ($25/месяц) или оптимизировать частоту синхронизации.

---

## 🚀 Использование на продакшене

**Ничего менять не нужно!** Edge Function будет работать так же:
- ✅ Автоматически масштабируется
- ✅ Высокая доступность (99.9% SLA на Pro плане)
- ✅ Безопасность (API ключи не видны в браузере)
- ✅ Мониторинг в Supabase Dashboard

---

## ❓ Решение проблем

### Ошибка: "Function not found"
- Убедитесь, что функция задеплоена: `supabase functions list`
- Проверьте, что используете правильный project-ref

### Ошибка: "CORS error"
- Убедитесь, что `USE_PROXY = true` в `wildberriesApiService.ts`
- Проверьте, что функция задеплоена и активна

### Ошибка: "Invalid API key"
- Проверьте, что API ключ правильно сохранен в настройках маркетплейса
- Убедитесь, что токен не содержит лишних пробелов

### Ошибка при деплое
- Проверьте, что вы вошли в Supabase: `supabase login`
- Проверьте, что проект связан: `supabase link --project-ref YOUR_REF`
- Убедитесь, что файл функции существует: `supabase/functions/wildberries-proxy/index.ts`

---

## 📝 Дополнительная информация

- **Документация Supabase Edge Functions**: https://supabase.com/docs/guides/functions
- **Мониторинг функций**: Supabase Dashboard → Edge Functions → Logs
- **Обновление функции**: Просто запустите `supabase functions deploy wildberries-proxy` снова

