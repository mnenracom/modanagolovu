# Настройка подтверждения email

## ✅ Что уже сделано:

1. ✅ Создан компонент `AuthCallback.tsx` для обработки подтверждения email
2. ✅ Добавлен маршрут `/auth/callback` в `App.tsx`
3. ✅ В `Register.tsx` настроен `emailRedirectTo` на `/auth/callback`

## 🔧 Что нужно настроить в Supabase:

### Шаг 1: Настройте Redirect URLs

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Выберите ваш проект
3. Перейдите в **Authentication** → **URL Configuration**
4. В разделе **Redirect URLs** добавьте:

   **Для разработки:**
   ```
   http://localhost:5173/auth/callback
   http://localhost:3000/auth/callback
   ```

   **Для продакшена (замените на ваш домен):**
   ```
   https://ваш-домен.com/auth/callback
   https://www.ваш-домен.com/auth/callback
   ```

5. Нажмите **Save**

### Шаг 2: Настройте Site URL

1. В том же разделе **URL Configuration**
2. Установите **Site URL**:

   **Для разработки:**
   ```
   http://localhost:5173
   ```

   **Для продакшена:**
   ```
   https://ваш-домен.com
   ```

3. Нажмите **Save**

### Шаг 3: Проверьте настройки Email

1. Перейдите в **Authentication** → **Settings**
2. В разделе **User Signups**:
   - ✅ **Confirm email** должен быть включён (если хотите требовать подтверждение)
   - ✅ **Enable Email Signup** должен быть включён

## 🧪 Тестирование:

1. Зарегистрируйте нового пользователя
2. Проверьте почту — должно прийти письмо с подтверждением
3. Нажмите на ссылку в письме
4. Должна открыться страница `/auth/callback` с сообщением об успешном подтверждении
5. Через 2 секунды вы будете автоматически перенаправлены на главную страницу

## ⚠️ Если ссылка ведёт на неправильный URL:

Если ссылка в письме ведёт на `localhost:3000` или другой порт:

1. Проверьте, на каком порту запущен ваш dev-сервер (обычно `5173` для Vite)
2. Обновите **Site URL** и **Redirect URLs** в Supabase Dashboard
3. Или измените `emailRedirectTo` в `Register.tsx` на правильный порт

## 🔍 Проверка работы:

После настройки Redirect URLs попробуйте:
1. Зарегистрировать нового пользователя
2. Перейти по ссылке из письма
3. Должна открыться страница `/auth/callback` с сообщением "Email подтверждён!"

Если всё работает, пользователь будет автоматически залогинен и перенаправлен на главную страницу.



