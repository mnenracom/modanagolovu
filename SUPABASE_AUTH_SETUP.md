# 🔐 Настройка Supabase Auth для админ-панели

## ✅ Что было сделано

Система аутентификации успешно мигрирована с кастомной на Supabase Auth.

### Созданные файлы:
- ✅ `src/hooks/useAuth.ts` - хук для управления аутентификацией
- ✅ `src/components/auth/LoginForm.tsx` - компонент формы входа
- ✅ Обновлен `src/components/admin/ProtectedRoute.tsx`
- ✅ Обновлен `src/components/admin/AdminLayout.tsx`
- ✅ Обновлен `src/App.tsx`

## 📋 Настройка пользователей в Supabase

### 1. Создание пользователя через Supabase Dashboard

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите ваш проект
3. Перейдите в **Authentication** → **Users**
4. Нажмите **Add user** → **Create new user**
5. Заполните:
   - **Email**: admin@example.com
   - **Password**: (надежный пароль)
   - **Auto Confirm User**: ✅ (включите для автоматического подтверждения)

### 2. Настройка ролей пользователя

Для установки роли пользователя нужно добавить метаданные:

1. После создания пользователя, нажмите на него для редактирования
2. В разделе **User Metadata** добавьте:
   ```json
   {
     "role": "admin"
   }
   ```
3. Или в **App Metadata**:
   ```json
   {
     "role": "admin"
   }
   ```

### 3. Доступные роли

- `admin` - полный доступ
- `manager` - управление товарами, заказами, клиентами
- `content` - управление товарами, категориями, баннерами
- `user` - минимальные права

### 4. Создание пользователей через SQL (альтернативный способ)

Если нужно создать пользователей программно, используйте SQL:

```sql
-- Создание пользователя через Supabase Auth (выполняется через Supabase Dashboard или API)
-- Для установки роли используйте функцию:

-- Обновление user_metadata для установки роли
UPDATE auth.users 
SET raw_user_meta_data = jsonb_build_object('role', 'admin')
WHERE email = 'admin@example.com';
```

### 5. Настройка RLS (Row Level Security) для админ-панели

**ВАЖНО**: Для правильной работы админ-панели необходимо настроить RLS политики.

Выполните SQL скрипт `fix_rls_policies.sql` в Supabase Dashboard → SQL Editor.

Этот скрипт:
- ✅ Включает RLS для таблиц `products`, `categories`, `banners`, `orders`
- ✅ Создает политики для админов, менеджеров и контент-менеджеров
- ✅ Создает политики для публичного доступа (клиенты могут видеть активные товары, категории, баннеры)

**Политики для админов:**
- `products` - доступ для `admin`, `manager`, `content`
- `categories` - доступ для `admin`, `manager`, `content`
- `banners` - доступ для `admin`, `manager`, `content`
- `orders` - доступ только для `admin`, `manager`

**Политики для клиентов (публичный доступ):**
- Товары в наличии (`in_stock = true`)
- Активные категории (`is_active = true`)
- Активные баннеры (`is_active = true`)

## 🔧 Конфигурация

### Настройка Supabase клиента

В `src/lib/supabase.ts` уже настроено:
```typescript
export const supabase = createClient(supabaseUrl, supabaseAnonKey, {
  auth: {
    persistSession: true, // Сохранение сессии
    autoRefreshToken: true, // Автоматическое обновление токена
    detectSessionInUrl: true, // Обнаружение сессии в URL
  },
});
```

## 🚀 Использование

### Вход в систему

1. Откройте `/admin/login`
2. Введите email и пароль пользователя из Supabase
3. После успешного входа вы будете перенаправлены на `/admin`

### Выход из системы

Кнопка "Выход" в админ-панели выполняет `signOut()` и перенаправляет на страницу входа.

## 📝 Примечания

- **Стили и UX сохранены**: Все существующие стили и пользовательский опыт остались без изменений
- **Совместимость**: Система работает с существующими компонентами и маршрутами
- **Безопасность**: Используется Supabase Auth с автоматическим обновлением токенов
- **Роли**: Роли пользователей хранятся в `user_metadata` или `app_metadata`

## ⚠️ Важно

1. Убедитесь, что в `.env` файле указаны правильные значения:
   ```
   VITE_SUPABASE_URL=your_supabase_url
   VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
   ```

2. После создания пользователя в Supabase, установите роль через метаданные

3. Старый контекст `AdminAuthContext` больше не используется, но файл сохранен для справки

## 🔄 Миграция существующих пользователей

Если у вас были тестовые пользователи:
- Старые данные в `localStorage` (admin_token, admin_user) больше не используются
- Создайте новых пользователей через Supabase Dashboard
- Установите роли через метаданные

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте консоль браузера на наличие ошибок
2. Убедитесь, что пользователь создан в Supabase
3. Проверьте, что роль установлена в метаданных пользователя
4. Убедитесь, что переменные окружения настроены правильно

