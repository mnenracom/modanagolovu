# Настройка виджета Почты России

## ✅ Виджет уже интегрирован!

Виджет Почты России успешно интегрирован в систему выбора доставки. Он автоматически активируется, когда API заблокирован или недоступен.

## 📋 Как это работает

1. **Пользователь вводит город** и нажимает "Найти точки выдачи"
2. **Система пробует API** через Edge Function
3. **Если API работает** → показывается список отделений через API
4. **Если API заблокирован (ошибка 417)** → автоматически показывается виджет
5. **Пользователь выбирает отделение** в виджете на карте
6. **Оформление заказа продолжается** с выбранным отделением

## 🔧 Текущие настройки виджета

Виджет использует следующие параметры:
- **ID виджета:** `60084` (по умолчанию)
- **Скрипт:** `https://widget.pochta.ru/map/widget/widget.js`
- **Высота:** 500px

## ⚙️ Как изменить ID виджета

Если у вас другой ID виджета (из личного кабинета Почты России):

### Вариант 1: Через код (быстро)

Откройте файл `src/pages/DeliverySelection.tsx` и найдите строку:

```tsx
<RussianPostWidget
  city={addressData.city}
  region={addressData.region}
  postalCode={addressData.postalCode}
  widgetId={60084} // ← Измените здесь на ваш ID
  onOfficeSelected={handleWidgetOfficeSelected}
/>
```

### Вариант 2: Через настройки доставки (рекомендуется)

1. Откройте админ-панель
2. Перейдите в **Доставка → Службы доставки**
3. Найдите службу **"Почта России"** (код: `russian_post`)
4. В разделе **"Настройки"** (JSON) добавьте:
   ```json
   {
     "widget_id": 60084
   }
   ```
5. Сохраните изменения

Система автоматически подхватит ID виджета из настроек.

## 📝 Структура данных виджета

Когда пользователь выбирает отделение в виджете, он возвращает объект с данными:

```typescript
{
  id: string;           // ID отделения
  name: string;         // Название отделения
  address: string;      // Полный адрес
  postalCode: string;   // Почтовый индекс
  index?: string;       // Альтернативный индекс
}
```

Эти данные автоматически преобразуются в формат `PostOffice` и используются для расчета доставки.

## 🎯 Преимущества виджета

- ✅ **Не требует белого списка IP** - работает напрямую с браузера
- ✅ **Интерактивная карта** - удобный выбор отделений
- ✅ **Автоматическое переключение** - система сама выбирает лучший способ
- ✅ **Надежность** - всегда есть запасной вариант

## 🔍 Отладка

Если виджет не загружается:

1. **Откройте консоль браузера** (F12)
2. **Проверьте ошибки** загрузки скрипта
3. **Убедитесь, что ID виджета правильный**
4. **Проверьте доступность** `https://widget.pochta.ru`

### Типичные ошибки:

- **"Функция виджета не найдена"** → Скрипт не загрузился, проверьте интернет
- **"Ошибка инициализации виджета"** → Неверный ID виджета
- **Виджет не отображается** → Проверьте, что контейнер `#ecom-widget` создан

## 📞 Поддержка

Если возникли проблемы с виджетом:
- **Email:** support@pochta.ru
- **Телефон:** 8-800-2005-888
- **Спросите:** Как получить доступ к виджету выбора отделений?

## ✅ Итог

Виджет полностью интегрирован и готов к использованию! Он автоматически активируется при проблемах с API, обеспечивая непрерывную работу системы доставки.


