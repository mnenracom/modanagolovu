# 📊 Сводка по интеграции ЮКассы

## 🏗️ Архитектура интеграции

### Общая схема работы:
```
Клиент (React) 
  → yookassaService.createPayment() 
  → Supabase Edge Function (create-yookassa-payment) 
  → API ЮКассы (https://api.yookassa.ru/v3/payments)
  → Возврат paymentUrl → Редирект на оплату
```

---

## 📁 Структура файлов

### 1. **Frontend (React/TypeScript)**

#### `src/services/yookassaService.ts`
**Назначение:** Клиентский сервис для работы с ЮКассой

**Основные функции:**
- `createPayment()` - создание платежа через Edge Function
- `checkPaymentStatus()` - проверка статуса платежа

**Ключевые моменты:**
- Использует Supabase Edge Function для обхода CORS
- Поддерживает тестовый и продакшн режимы
- Обрабатывает ошибки с детальными сообщениями

**Параметры `createPayment()`:**
```typescript
{
  gateway: PaymentGateway,      // Настройки платежной системы из БД
  amount: number,                // Сумма в рублях
  orderId: string,               // ID заказа в БД
  orderNumber: string,          // Номер заказа для отображения
  description: string,           // Описание платежа
  returnUrl: string              // URL возврата после оплаты
}
```

**Возвращает:**
```typescript
{
  paymentUrl: string,            // URL для редиректа на оплату
  paymentId: string              // ID платежа в ЮКассе
}
```

**Обработка ошибок:**
- Проверка наличия ключей (shopId, secretKey)
- Обработка ошибок Edge Function
- Проверка наличия paymentUrl в ответе

---

#### `src/pages/Payment.tsx`
**Назначение:** Страница оплаты для розничных заказов

**Поток работы:**
1. Загружает данные доставки из `sessionStorage`
2. Загружает активные платежные системы
3. Пользователь заполняет контактные данные
4. Выбирает способ оплаты (ЮКасса)
5. При нажатии "Оплатить":
   - Создается заказ в БД
   - Вызывается `yookassaService.createPayment()`
   - Заказ обновляется с `payment_external_id` и `payment_url`
   - Редирект на `paymentUrl`

**Критические моменты:**
- Проверка наличия товаров в корзине
- Проверка типа заказа (только розница)
- Валидация данных доставки
- Обработка ошибок создания платежа

---

#### `src/pages/Checkout.tsx`
**Назначение:** Страница оформления оптовых заказов

**Отличие от Payment.tsx:**
- Для оптовых заказов
- Может использовать ЮКассу для онлайн-оплаты
- Аналогичный поток создания платежа

---

#### `src/pages/CheckoutSuccess.tsx`
**Назначение:** Страница успешного оформления заказа

**Функционал:**
- Проверяет статус платежа через `yookassaService.checkPaymentStatus()`
- Обновляет статус заказа на `paid` если платеж успешен
- Отображает информацию о заказе

---

### 2. **Backend (Supabase Edge Function)**

#### `supabase/functions/create-yookassa-payment/index.ts`
**Назначение:** Серверная функция для создания платежа (обход CORS)

**Технология:** Deno runtime

**Параметры запроса:**
```typescript
{
  shopId: string,               // ID магазина ЮКассы
  secretKey: string,            // Секретный ключ
  amount: number,               // Сумма в рублях
  orderId: string,              // ID заказа
  orderNumber: string,          // Номер заказа
  description: string,          // Описание
  returnUrl: string,            // URL возврата
  testMode: boolean             // Тестовый режим
}
```

**Формируемый запрос к API ЮКассы:**
```json
{
  "amount": {
    "value": "350.00",
    "currency": "RUB"
  },
  "confirmation": {
    "type": "redirect",
    "return_url": "https://your-site.com/checkout/success?orderId=123"
  },
  "description": "Заказ №123",
  "metadata": {
    "orderId": "123",
    "orderNumber": "123",
    "testMode": "false"
  },
  "capture": true
}
```

**Заголовки запроса:**
```
Content-Type: application/json
Idempotence-Key: {orderId}-{timestamp}
Authorization: Basic {base64(shopId:secretKey)}
```

**Обработка ошибок:**
- Валидация параметров (400)
- Проверка формата Shop ID (400)
- Обработка ошибок API ЮКассы (статус ответа)
- Проверка наличия confirmation_url (500)
- Детальное логирование в catch блоке

**Возвращаемый ответ:**
```json
{
  "paymentUrl": "https://yoomoney.ru/checkout/payments/v2/...",
  "paymentId": "2c5d8b3a-0001-0002-0003-000000000000"
}
```

---

### 3. **База данных**

#### Таблица `payment_gateways`
**Структура:**
- `id` - ID записи
- `code` - Код платежной системы (`'yookassa'`)
- `name` - Название
- `display_name` - Отображаемое название
- `shop_id` - ID магазина ЮКассы
- `secret_key` - Секретный ключ (продакшн)
- `test_shop_id` - Тестовый Shop ID
- `test_secret_key` - Тестовый секретный ключ
- `test_mode` - Флаг тестового режима
- `active` - Включена ли платежная система
- `api_url` - URL API (по умолчанию `https://api.yookassa.ru`)
- `website_url` - URL сайта (по умолчанию `https://yookassa.ru`)

#### Таблица `orders`
**Связанные поля:**
- `payment_gateway_id` - ID платежной системы
- `payment_external_id` - ID платежа в ЮКассе
- `payment_url` - URL для оплаты
- `payment_status` - Статус оплаты (`pending`, `paid`, `failed`)
- `payment_method` - Способ оплаты (`online`, `cash`)

---

## 🔄 Полный поток оплаты

### 1. Инициация платежа
```
Пользователь на странице /payment
  ↓
Заполняет контактные данные
  ↓
Выбирает ЮКассу как способ оплаты
  ↓
Нажимает "Оплатить"
  ↓
Создается заказ в БД (status: 'pending', payment_status: 'pending')
  ↓
Вызывается yookassaService.createPayment()
  ↓
Отправляется запрос в Supabase Edge Function
  ↓
Edge Function делает POST запрос к API ЮКассы
  ↓
ЮКасса возвращает paymentUrl и paymentId
  ↓
Заказ обновляется: payment_external_id, payment_url
  ↓
Редирект на paymentUrl (страница оплаты ЮКассы)
```

### 2. После оплаты
```
Пользователь оплачивает на странице ЮКассы
  ↓
Редирект на returnUrl (/checkout/success?orderId=123)
  ↓
CheckoutSuccess.tsx загружает заказ
  ↓
Вызывается yookassaService.checkPaymentStatus()
  ↓
GET запрос к API ЮКассы для проверки статуса
  ↓
Если статус 'succeeded' → обновление payment_status = 'paid'
```

---

## ⚙️ Настройки

### Обязательные параметры:
1. **Shop ID** - числовой идентификатор магазина
2. **Secret Key** - секретный ключ для авторизации

### Тестовый режим:
- Включить переключатель `test_mode` в админ-панели
- Использовать тестовые ключи:
  - Тестовый Shop ID: `381764678` (стандартный)
  - Тестовый Secret Key: получить в личном кабинете ЮКассы

### Админ-панель:
- Путь: `/admin/delivery` → "Платежные системы"
- Настройка через `src/pages/admin/PaymentGateways.tsx`

---

## 🐛 Возможные ошибки и их причины

### 1. "Не настроены ключи ЮКассы"
**Причина:** Отсутствуют `shopId` или `secretKey` в настройках
**Решение:** Заполнить поля в админ-панели

### 2. "Ошибка создания платежа через Edge Function"
**Возможные причины:**
- Edge Function не задеплоена
- Неверный формат Shop ID (не число)
- Недостаточно параметров в запросе
- Ошибка сети при вызове Edge Function

**Решение:**
- Проверить деплой: `npx supabase functions deploy create-yookassa-payment`
- Проверить логи Edge Function в Supabase Dashboard
- Проверить формат Shop ID (должно быть число)

### 3. "Не получен URL для оплаты от ЮКассы"
**Причина:** API ЮКассы не вернул `confirmation.confirmation_url`
**Возможные причины:**
- Неверные ключи авторизации
- Ошибка на стороне ЮКассы
- Неверный формат запроса

**Решение:**
- Проверить правильность ключей
- Проверить логи Edge Function для деталей ошибки
- Проверить статус ответа от API ЮКассы

### 4. "Ошибка создания платежа: 401"
**Причина:** Неверная авторизация (неправильные ключи)
**Решение:** Проверить Shop ID и Secret Key

### 5. "Ошибка создания платежа: 400"
**Причина:** Неверный формат запроса
**Возможные причины:**
- Неверный формат суммы
- Отсутствуют обязательные поля
- Неверный формат return_url

**Решение:**
- Проверить формат `amount` (должно быть число с 2 знаками после запятой)
- Проверить наличие всех обязательных полей
- Проверить формат `returnUrl`

### 6. "Ошибка проверки статуса: 401"
**Причина:** Неверные ключи при проверке статуса
**Решение:** Проверить ключи в настройках платежной системы

---

## 📝 Логирование

### Frontend (консоль браузера):
- `console.error('Ошибка создания платежа ЮКассы:', error)` - в `yookassaService.ts`
- `console.error('Ошибка проверки статуса платежа:', error)` - в `yookassaService.ts`
- `console.error('Ошибка создания платежа:', paymentError)` - в `Payment.tsx` и `Checkout.tsx`

### Backend (Supabase Edge Function):
- `console.error('Ошибка в функции create-yookassa-payment:', error)`
- `console.error('Тип ошибки:', error.constructor?.name)`
- `console.error('Сообщение:', error.message)`
- `console.error('Стек ошибки:', error.stack)`

**Где смотреть логи:**
- Supabase Dashboard → Edge Functions → `create-yookassa-payment` → Logs

---

## 🔍 Диагностика проблем

### Шаг 1: Проверка настроек
1. Открыть админ-панель → Платежные системы
2. Проверить, что ЮКасса включена
3. Проверить наличие Shop ID и Secret Key
4. Проверить режим (тестовый/продакшн)

### Шаг 2: Проверка Edge Function
1. Supabase Dashboard → Edge Functions
2. Проверить, что `create-yookassa-payment` задеплоена
3. Проверить логи на наличие ошибок

### Шаг 3: Проверка запроса
1. Открыть DevTools → Network
2. Найти запрос к `create-yookassa-payment`
3. Проверить:
   - Статус ответа
   - Тело запроса (параметры)
   - Тело ответа (ошибки)

### Шаг 4: Проверка API ЮКассы
1. Проверить логи в личном кабинете ЮКассы
2. Проверить статус API (включен ли)
3. Проверить правильность ключей

---

## 📋 Чек-лист для нейросети

При диагностике ошибки оплаты проверьте:

1. ✅ **Настройки в админ-панели:**
   - Платежная система включена?
   - Заполнены Shop ID и Secret Key?
   - Правильный режим (тестовый/продакшн)?

2. ✅ **Edge Function:**
   - Функция задеплоена?
   - Есть ли ошибки в логах?
   - Правильные параметры в запросе?

3. ✅ **API ЮКассы:**
   - Правильные ключи авторизации?
   - API включен в личном кабинете?
   - Есть ли ошибки в логах ЮКассы?

4. ✅ **Формат данных:**
   - Сумма в правильном формате (число с 2 знаками)?
   - returnUrl валидный?
   - Все обязательные поля присутствуют?

5. ✅ **Сеть:**
   - CORS ошибки?
   - Таймауты?
   - Доступность API ЮКассы?

---

## 🔗 Ссылки на документацию

- [Документация ЮКассы](https://yookassa.ru/developers)
- [API ЮКассы](https://yookassa.ru/developers/api)
- [Тестирование платежей](https://yookassa.ru/developers/payment-acceptance/testing-and-going-live/testing)
- [Личный кабинет ЮКассы](https://yookassa.ru/my)

---

## 📄 Файлы для передачи нейросети

1. `src/services/yookassaService.ts` - клиентский сервис
2. `supabase/functions/create-yookassa-payment/index.ts` - Edge Function
3. `src/pages/Payment.tsx` - страница оплаты
4. `src/pages/CheckoutSuccess.tsx` - страница успеха
5. `YOOKASSA_SETUP.md` - инструкция по настройке
6. Этот файл (`YOOKASSA_INTEGRATION_SUMMARY.md`) - сводка

---

## 💡 Важные замечания

1. **CORS:** Все запросы к API ЮКассы идут через Edge Function для обхода CORS
2. **Безопасность:** Секретные ключи хранятся в БД, не в коде
3. **Тестовый режим:** Используйте тестовые ключи для проверки
4. **Idempotence-Key:** Генерируется как `{orderId}-{timestamp}` для предотвращения дублей
5. **Capture:** Установлен в `true` для автоматического подтверждения платежа
6. **Статус платежа:** Проверяется на странице успеха, но не автоматически обновляется

---

**Дата создания сводки:** 2024
**Версия интеграции:** 1.0

