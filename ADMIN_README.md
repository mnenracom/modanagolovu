# Админ-панель

## Структура проекта

### Frontend (React)

#### Компоненты
- `src/contexts/AdminAuthContext.tsx` - Контекст для управления авторизацией админа
- `src/components/admin/AdminLayout.tsx` - Layout с сайдбаром и хедером
- `src/components/admin/ProtectedRoute.tsx` - Защищенный роут для админ-страниц

#### Страницы
- `src/pages/admin/Login.tsx` - Страница входа в админ-панель
- `src/pages/admin/Dashboard.tsx` - Дашборд с метриками
- `src/pages/admin/Products.tsx` - CRUD для управления товарами

#### API
- `src/lib/api.ts` - Настройка Axios и API endpoints

### Backend (MongoDB Models)

#### Модели
- `server/models/Product.js` - Модель товара
- `server/models/User.js` - Модель пользователя

## Установка зависимостей

```bash
npm install axios
```

## Использование

### Тестовые данные для входа

- **Email**: `admin@example.com`
- **Пароль**: `admin123`

### Маршруты админ-панели

- `/admin/login` - Страница входа
- `/admin` - Дашборд
- `/admin/products` - Управление товарами

## Настройка API

В файле `src/lib/api.ts` измените `API_BASE_URL` на URL вашего бэкенда:

```typescript
const API_BASE_URL = import.meta.env.VITE_API_URL || 'http://localhost:3000/api';
```

Или создайте файл `.env`:

```
VITE_API_URL=http://localhost:3000/api
```

## Функциональность

### ✅ Реализовано

1. **Layout с сайдбаром и хедером**
   - Адаптивный сайдбар с меню
   - Хедер с информацией о пользователе
   - Кнопка выхода

2. **Страница логина**
   - Форма входа с валидацией
   - Обработка ошибок
   - Сохранение токена в localStorage

3. **Дашборд**
   - Метрики (товары, заказы, пользователи, выручка)
   - Карточки статистики
   - Место для графиков и дополнительной информации

4. **CRUD для товаров**
   - Список товаров в таблице
   - Поиск по товарам
   - Создание нового товара
   - Редактирование товара
   - Удаление товара

5. **Context API**
   - Управление состоянием авторизации
   - Хранение токена и данных пользователя

6. **Axios**
   - Настроенный клиент с базовым URL
   - Интерцепторы для токена авторизации
   - Обработка ошибок 401

7. **MongoDB Models**
   - Модель Product с полями товара
   - Модель User с хешированием паролей

### 🔄 TODO (для подключения к реальному API)

1. Заменить моковые данные на реальные API запросы
2. Добавить загрузку изображений для товаров
3. Реализовать пагинацию для списка товаров
4. Добавить валидацию форм с помощью Zod
5. Добавить обработку ошибок с toast-уведомлениями
6. Реализовать остальные страницы (Заказы, Пользователи, Настройки)

## Следующие шаги

1. Установите axios: `npm install axios`
2. Настройте бэкенд API
3. Подключите реальные API endpoints
4. Добавьте дополнительные функции по необходимости










