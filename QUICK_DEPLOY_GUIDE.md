# 🚀 Быстрая инструкция по деплою Edge Function

## Минимальные шаги (5 минут)

1. **Установите CLI (выберите один способ):**

   **Способ 1 - Через Scoop (рекомендуется):**
   ```powershell
   scoop bucket add supabase https://github.com/supabase/scoop-bucket.git
   scoop install supabase
   ```
   
   **Способ 2 - Через npx (без установки):**
   ```bash
   # Используйте npx перед каждой командой supabase
   npx supabase@latest login
   ```
   
   **Способ 3 - Прямая загрузка:**
   - Скачайте с https://github.com/supabase/cli/releases
   - Распакуйте и добавьте в PATH

2. **Войдите (используйте npx):**
   ```bash
   npx supabase@latest login
   ```

3. **Найдите Project Reference:**
   - Откройте https://app.supabase.com/
   - Settings → General → Reference ID
   - Скопируйте ID (например: `abcdefghijklmnop`)

4. **Свяжите проект:**
   ```bash
   cd S:\modna-golovu-optom-main
   npx supabase@latest link --project-ref ВАШ_REFERENCE_ID
   ```

5. **Задеплойте:**
   ```bash
   npx supabase@latest functions deploy wildberries-proxy
   ```

6. **Готово!** Тестируйте синхронизацию в админ-панели.

---

## ✅ Проверка

После деплоя проверьте:
- `supabase functions list` — должна быть функция `wildberries-proxy`
- В админ-панели попробуйте синхронизировать данные
- В консоли браузера (F12) не должно быть CORS ошибок

---

## 🔄 Обновление функции

Если изменили код функции, просто запустите снова:
```bash
supabase functions deploy wildberries-proxy
```

---

## ❓ Проблемы?

Смотрите подробную инструкцию в `DEPLOY_EDGE_FUNCTION.md`
