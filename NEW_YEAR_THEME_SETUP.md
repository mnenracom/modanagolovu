# Настройка новогодней темы

## 🎄 Кастомные изображения

### Мишура для Header

Поместите ваше изображение мишуры в файл:
```
public/tinsel-header.png
```

**Рекомендуемые характеристики:**
- **Ширина:** 1920px (или больше для повторения)
- **Высота:** 32-64px
- **Формат:** PNG с прозрачным фоном
- **Содержание:** Горизонтальная полоса мишуры/гирлянды, которая будет повторяться по всей ширине header

Если файл не загружен, будет использована CSS гирлянда по умолчанию.

### Ёлки по краям

Поместите изображения ёлок в файлы:
```
public/christmas-tree-left.png  (левая ёлка)
public/christmas-tree-right.png (правая ёлка)
```

**Рекомендуемые характеристики:**
- **Ширина:** 120-200px
- **Высота:** 400-600px (или больше, будет масштабироваться)
- **Формат:** PNG с прозрачным фоном
- **Содержание:** Вертикальная ёлка, которая будет отображаться по краям экрана

Если файлы не загружены, будут использованы простые CSS ёлки.

## ✨ Что улучшено

1. **Убрано конфетти** - больше не лагает
2. **Оптимизированы снежинки** - меньше элементов, лучше производительность
3. **Добавлены ёлки по краям** - только на десктопе (lg+), с анимацией шариков
4. **Кастомная мишура** - можно загрузить своё PNG изображение
5. **GPU ускорение** - все анимации используют `transform: translateZ(0)`
6. **Оптимизация для мобильных** - меньше элементов, ёлки скрыты

## 🎯 Производительность

- Используется `will-change` для оптимизации анимаций
- `contain: layout style paint` для изоляции рендеринга
- `backface-visibility: hidden` для лучшей производительности
- Меньше элементов на мобильных устройствах
- Ёлки показываются только на больших экранах (lg+)

## 📝 Примечания

- Все изображения опциональны - если не загружены, используются CSS fallback'и
- Ёлки автоматически скрываются на мобильных устройствах для лучшей производительности
- Снежинки оптимизированы и не должны вызывать лагов

