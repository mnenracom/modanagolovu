# Быстрый старт - команды для установки

## Для Windows (PowerShell или CMD):

```powershell
# 1. Перейдите в папку проекта
cd путь\к\проекту\modna-golovu-optom-main

# 2. Установите все зависимости
npm install

# 3. Создайте файл .env (скопируйте и вставьте в PowerShell)
@"
VITE_SUPABASE_URL=ваш_url_из_supabase
VITE_SUPABASE_ANON_KEY=ваш_anon_key_из_supabase
"@ | Out-File -FilePath .env -Encoding utf8

# 4. Откройте файл .env и замените значения на реальные из Supabase
notepad .env

# 5. Запустите проект
npm run dev
```

## Для Mac/Linux (Terminal):

```bash
# 1. Перейдите в папку проекта
cd путь/к/проекту/modna-golovu-optom-main

# 2. Установите все зависимости
npm install

# 3. Создайте файл .env
cat > .env << EOF
VITE_SUPABASE_URL=ваш_url_из_supabase
VITE_SUPABASE_ANON_KEY=ваш_anon_key_из_supabase
EOF

# 4. Откройте файл .env и замените значения на реальные из Supabase
nano .env
# или
code .env

# 5. Запустите проект
npm run dev
```

## Минимальный набор команд (если .env уже настроен):

```bash
npm install
npm run dev
```

## Где найти данные для .env:

1. Откройте https://supabase.com/dashboard
2. Выберите ваш проект
3. Перейдите в **Settings** → **API**
4. Скопируйте:
   - **Project URL** → `VITE_SUPABASE_URL`
   - **anon public** key → `VITE_SUPABASE_ANON_KEY`

## После запуска:

Откройте браузер: **http://localhost:5173**

---

**Всё! Проект готов к работе! 🎉**
