# Деплой Edge Function для API Почты России

## ⚠️ Важно: Edge Function должна быть задеплоена!

Ошибка "Failed to send a request to the Edge Function" означает, что функция `russian-post-api` не задеплоена на Supabase.

## 🚀 Как задеплоить Edge Function

### Вариант 1: Через Supabase CLI (рекомендуется)

1. **Установите Supabase CLI** (если еще не установлен):
   ```bash
   npm install -g supabase
   ```

2. **Войдите в Supabase**:
   ```bash
   supabase login
   ```

3. **Свяжите проект с Supabase**:
   ```bash
   supabase link --project-ref YOUR_PROJECT_REF
   ```
   Где `YOUR_PROJECT_REF` - это ID вашего проекта Supabase (можно найти в настройках проекта)

4. **Задеплойте функцию**:
   ```bash
   supabase functions deploy russian-post-api
   ```

### Вариант 2: Через Supabase Dashboard

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Выберите ваш проект
3. Перейдите в **Edge Functions**
4. Нажмите **"Deploy a new function"**
5. Загрузите папку `supabase/functions/russian-post-api`
6. Или используйте CLI команду из терминала в Dashboard

## ✅ Проверка деплоя

После деплоя проверьте:

1. В Supabase Dashboard → Edge Functions должна быть функция `russian-post-api`
2. Статус функции должен быть "Active"
3. Попробуйте снова использовать поиск точек выдачи

## 🔧 Если функция уже задеплоена

Если функция задеплоена, но ошибка сохраняется:

1. **Проверьте имя функции** - должно быть точно `russian-post-api`
2. **Проверьте права доступа** - функция должна быть доступна для вызова
3. **Проверьте логи** в Supabase Dashboard → Edge Functions → Logs
4. **Передеплойте функцию**:
   ```bash
   supabase functions deploy russian-post-api --no-verify-jwt
   ```

## 📝 Структура файлов

Убедитесь, что структура файлов правильная:

```
supabase/
  functions/
    russian-post-api/
      index.ts  ← главный файл функции
```

## 🐛 Отладка

Если проблема сохраняется:

1. Откройте консоль браузера (F12)
2. Проверьте детали ошибки
3. Откройте Supabase Dashboard → Edge Functions → Logs
4. Посмотрите логи выполнения функции

## 📚 Дополнительная информация

- [Документация Supabase Edge Functions](https://supabase.com/docs/guides/functions)
- [Документация API Почты России](https://otpravka.pochta.ru/help)


