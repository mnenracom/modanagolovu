# Финальная настройка регистрации пользователей

## ✅ Что уже сделано:

1. ✅ Созданы записи в `public.users` для существующих пользователей
2. ✅ Таблица `users` настроена
3. ✅ RLS политики настроены

## 🔧 Что нужно сделать сейчас:

### Шаг 1: Исправить триггер для новых регистраций

Выполните скрипт `fix_trigger_with_error_handling.sql` в Supabase SQL Editor.

Этот скрипт:
- Пересоздаст функцию триггера с улучшенной обработкой ошибок
- Добавит логирование для диагностики
- Пересоздаст триггер `on_auth_user_created`

### Шаг 2: Проверить триггер

Выполните скрипт `test_registration_trigger.sql`, чтобы убедиться, что:
- Функция `handle_new_user` существует и настроена правильно
- Триггер `on_auth_user_created` существует
- Владелец функции имеет права `SECURITY DEFINER`

### Шаг 3: Протестировать регистрацию

1. Зарегистрируйте нового тестового пользователя
2. Проверьте, что пользователь появился в:
   - `Authentication → Users` (auth.users)
   - В таблице `public.users` (выполните: `SELECT * FROM public.users ORDER BY created_at DESC LIMIT 5;`)

### Шаг 4: Проверить логи (если что-то не работает)

1. В Supabase Dashboard → **Database** → **Logs** → **Postgres Logs**
2. Найдите сообщения, связанные с триггером `handle_new_user`
3. Проверьте наличие ошибок или предупреждений

## 📝 Скрипты для выполнения:

1. **`fix_trigger_with_error_handling.sql`** - исправляет триггер
2. **`test_registration_trigger.sql`** - проверяет триггер
3. **`fix_users_table_rls_final.sql`** - настраивает RLS (если ещё не выполнено)

## 🎯 Ожидаемый результат:

После выполнения всех шагов:
- ✅ Новые пользователи автоматически создаются в `public.users` при регистрации
- ✅ Триггер работает без ошибок
- ✅ Логи показывают успешное создание записей

## ⚠️ Если триггер всё ещё не работает:

1. Проверьте логи Postgres на наличие ошибок
2. Убедитесь, что функция имеет `SECURITY DEFINER`
3. Временно отключите RLS для тестирования:
   ```sql
   ALTER TABLE users DISABLE ROW LEVEL SECURITY;
   ```
4. Попробуйте зарегистрировать нового пользователя
5. Если работает, включите RLS обратно и проверьте политики

